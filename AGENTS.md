# AGENTS.md — Exegete 에이전트 실행 지침

이 저장소는 Claude·Codex 등 **어떤 코딩 에이전트로도** 쓸 수 있다. 핵심 도구(`src/*.py`)는 순수 파이썬이라 에이전트와 무관하게 실행된다. 방법론·라우팅·감사·안전 지침의 **단일 진실 원천(SSOT)은 `CLAUDE.md`** 이다. 이 파일은 `AGENTS.md`를 읽는 에이전트(Codex 등)를 위한 진입점이며, 아래 문서를 반드시 읽고 그대로 따른다.

## 반드시 읽고 따를 문서
- **`CLAUDE.md`** — 요청 라우팅·실행 모드·분석 절차·모드별 지침(주해 / 묵상 / 단어·주제 / 절기 / 시리즈 / 병행본문 / 배경 / **주제·자료 기반 설교** 등)
- **`docs/METHODOLOGY.md`** — 4단계 주해 방법론(구조·문헌학·신학·설교)
- **`harness/exegesis_audit.md`** — 완료 전 사실 감사 게이트(설교 예화·인용 감사 강도 포함)
- **`docs/CARE_SAFETY.md`** — 돌봄·안전 경계

## 절대 철칙 (요약 — 상세·예외는 위 문서가 우선)
1. **본문은 `lookup.py`로 추출해 인용.** 기억으로 본문·구절을 지어내지 않는다(환각 1순위 차단).
2. **원어·사전·관주는 데이터에서만.** `greek_lookup.py`/`hebrew_lookup.py`(`--lex`)·`xref.py` 실제 출력만 근거로 쓰고, 없거나 모호하면 `[확인 필요]`.
3. **신학 전통은 사용자 지정 우선**, 갈리는 쟁점(세례·예정·종말론)은 병기. 감사는 신학·적용의 옳고 그름을 판정하지 않는다.
4. **완료·내보내기 전 감사**(`harness/exegesis_audit.md`). `PASS` 또는 동의가 기록된 `WARN`만 내보낸다. 설교 예화·인용은 사용자가 고른 감사 강도(강단용/표준/출판용)를 따르되, 본문·원어·관주는 강도와 무관하게 엄격하다.
5. **안전·개인정보**: 자해·자살·즉시 폭력·학대는 `safety_first` 우선, 이메일·전화·주민번호는 `privacy_review`. 요청·자료 원문을 저장·재출력하지 않는다.
6. **저작권 본문**(개역개정·NA28·BHS 등)을 내려받거나 저장소에 넣거나 대량 전재하지 않는다.

## 실행 환경 메모
- Windows에서 스크립트는 `PYTHONUTF8=1`로 실행한다(한글 입출력 깨짐 방지).
- 로컬 요청 분류만 필요하면 `python src/router.py "<요청>"` — 계획 JSON만 출력하고, 도구를 실행하거나 요청 원문을 저장하지 않는다.
- 데이터 상태(원어·사전·관주 미설치 등) 게이트와 라우팅 골든셋은 `tests/routing_cases.md`를 따른다.
