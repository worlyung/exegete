# Exegete 의존성
#
# 핵심 도구(lookup / greek_lookup / hebrew_lookup / word_search / series /
# liturgical / background)는 파이썬 표준 라이브러리만으로 동작합니다.
#
# 아래는 선택 기능용 의존성입니다.

# 워드(.docx) 내보내기 — src/export_docx.py
python-docx>=1.1.0
