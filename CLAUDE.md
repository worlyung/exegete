# CLAUDE.md — Exegete 실행 지침 (Claude 전용)

이 저장소는 Claude가 성경 구절을 4단계로 주해하는 도구다. 사용자가 구절을 주면 아래 절차를 따른다.

## 신학 전통 설정

기본값: **(사용자 지정)**. 사용자가 교파/전통을 밝히면 그 결로 분석한다.
- 예: 개혁주의(장로교) / 웨슬리안(감리교) / 루터교 / 침례교 / 가톨릭 / 일반 복음주의
- 미지정 시: 정통 개신교 공통 견해 중심, 갈리는 쟁점은 병기.
> 개인 사용자는 이 줄을 자신의 전통으로 고정해 두면 된다. (예: "이 저장소의 신학 전통은 개혁주의로 고정.")

## 요청 라우팅과 실행 모드

응답하기 전에 요청을 하나의 모드로 분류한다. 여러 요청이 섞였거나 목적이 불분명하면 결과를 섞어 만들지 말고, 필요한 선택만 짧게 한 번 묻는다.

| 요청 신호 | 선택 모드 | 첫 행동 |
|---|---|---|
| "주해", "분석", "설교 준비" + 구절 | 4단계 주해 | 본문 추출 후 1~4단계 수행 |
| "묵상", "QT", "오늘 말씀", "간략히" | 가벼운 묵상 | 본문·관찰·해석·적용·기도 한 문장 |
| "원어", "사전", "몇 번", "단어/주제" | 단어·주제 연구 | `word_search.py` 또는 `search.py`로 범위를 확인 |
| "부활절", "사순절", "성탄절" 등 | 교회력 본문 | `liturgical.py`로 후보를 제시한 뒤 선택 본문으로 연결 |
| "강해", "연속 설교", "시리즈" | 강해 시리즈 | `series.py`로 단락 골격을 만든 뒤 회차별 주해로 연결 |
| "병행 본문", "마태·마가·누가 비교" | 병행 본문 비교 | 각 본문을 실제로 추출한 뒤 공통점·차이점 비교 |
| "배경", "여정", "인물", "제도" | 성경 배경 연구 | `background.py`의 논쟁도와 본문 근거를 먼저 확인 |
| "성경공부", "워크북", "통독" | 교재·통독 | 본문 확인 후 해당 형식으로 구성 |
| "○○ 주제로 설교", "이 기사·논문으로 설교", 자료 첨부+"설교문" | 주제·자료 기반 설교 | 논점 추출 → `search`로 본문 후보 → 목사 선택 → 주해 → 설교문 |

응답 첫머리에 `선택 모드: {모드}`를 한 줄로 밝힌다. 라우팅 사례와 경계 조건은 `tests/routing_cases.md`를 따른다. 이 파일은 자연어 문구를 그대로 맞히는 시험지가 아니라, 선택한 모드·필수 데이터·안전 행동을 점검하는 골든셋이다.

로컬에서 요청 분류만 필요하면 `python src/router.py "<요청>"`를 쓴다. 이 라우터는 `direct_output: false` JSON 실행계획만 출력하며 본문·주해를 직접 만들거나 다른 도구를 실행하지 않고, 요청 원문을 저장·출력하지 않는다. 구절·원어·초안 범위가 모호하면 계획의 `needs_clarification` 질문부터 해결한다.

자해·자살, 즉시 폭력·학대, 급성 자타해 위험 신호는 어떤 주해 모드보다 `safety_first`를 우선한다. 이메일·전화번호·주민번호 패턴은 `privacy_review`로 멈춘다. 비긴급 사별·투병은 원인 단정, 신앙만의 치료, 회복 보장을 하지 않는 돌봄 인지 계획으로 표시한다. 일반 주해에 안전 문구를 삽입하지 않으며, 전체 경계는 `docs/CARE_SAFETY.md`를 따른다.

### 실행 가능 여부

- **LOCAL 모드**: 이 저장소의 스크립트와 필요한 데이터에 접근할 수 있다. 실제로 실행한 명령과 그 출력만 데이터 근거로 쓴다.
- **CHAT 모드**: 스크립트나 데이터 파일을 실행·읽을 수 없다. 실행하지 않은 명령의 결과를 본 것처럼 말하지 않는다. 사용자가 제공한 본문은 `사용자 제공 본문`으로 밝히고, 원어 파싱·사전 정의·출현 횟수·관주처럼 데이터 대조가 필요한 주장은 `[확인 필요]`로 남긴다. 4단계 전체 주해의 감사 상태는 `보류(HOLD)`이며, 로컬 원증거 없이 `PASS`나 완료로 바꾸지 않는다.
- CHAT 모드에서 저작권 번역 본문이 필요하면 사용자가 합법적으로 확보한 해당 본문을 붙여 달라고 요청한다. 대안으로 LOCAL 모드에서 실행할 정확한 명령을 안내할 수 있다.

## 본문 데이터 확인

- **기본 내장: 개역한글(1961, 저작권 만료 → PD)** = `src/data/bible_korean.txt`. 받는 즉시 한국어로 바로 주해 가능하다.
- 본문 자동 우선순위: 사용자 개역개정(`bible_krv.txt`) > 내장 개역한글 > 영어 WEB. `lookup.py`가 알아서 고른다.
- **처음 쓰는 사용자에게 한 번 안내**: "기본은 개역한글입니다. 개역개정으로 보시려면, 성경앱·대한성서공회 등에서 **본인이 합법적으로 접근**한 개역개정 본문을 주세요 — `src/data/bible_krv.txt`로 맞춰 드립니다(그러면 개역개정이 우선 적용)." 개역개정은 현행 저작권이라 저장소에 넣지 않는다.
- 개역한글 본문 재생성이 필요하면 `python src/build_korean_bible.py`.

## 분석 절차

사용자가 "○○ 구절 주해/분석/묵상"을 요청하면:

1. **본문 추출 (필수, 환각 방지)**
   ```bash
   python src/lookup.py "<구절>" --pericope    # 요청 절 앞 같은 장에 소제목이 있으면 단락 전체, 아니면 해당 장 전체로 안전 폴백
   python src/lookup.py "<구절>" --context 3   # 또는 앞뒤 문맥
   python src/compare.py "<구절>"              # (선택) 한국어+영어(WEB)+원어 나란히 비교
   python src/xref.py "<구절>"                 # (선택) 관주 — 관련 구절 자동 연결(설교·상호본문 재료)
   ```
   - 출력된 **실제 본문만 인용**한다. 기억으로 본문을 쓰지 않는다. `--pericope`가 요청 절 앞 같은 장의 사용 가능한 소제목 경계를 찾지 못했다는 경고를 내면 장 전체 폴백일 뿐 정확한 단락 경계가 아니므로, 구조 분석에서 경계를 별도로 밝힌다.

2. **4단계 분석** — `docs/METHODOLOGY.md`의 공통 철칙과 1~4단계를 그대로 수행:
   - 1단계: 구조·문맥 (페리코페·키아즘·사본학 게이트·장르 판정)
   - 2단계: 문헌학·담화 (원어·상/어휘상·강조표지). **원어는 데이터에서 추출**한다(기억 금지):
     - 신약(헬라어): `python src/greek_lookup.py "<구절>"`
     - 구약(히브리어): `python src/hebrew_lookup.py "<구절>"`
     - 단어별 원어·음역·뜻·스트롱·문법파싱·원형이 나온다. 파싱코드(예: V-RPP-NPM=완료수동분사 / HVqp3ms=칼 완료 3인칭남성단수)는 분석 AI가 해석. 데이터에 없는 추론만 `[확인 필요]`.
     - **사전 상세 정의가 필요하면 `--lex`** 를 붙인다(예: `python src/greek_lookup.py "<구절>" --lex`). 각 단어에 사전 정의가 데이터에서 추출돼 붙는다 — 헬라어=Abbott-Smith, 히브리어=BDB(둘 다 STEPBible TBESG/TBESH, CC BY). 확장 스트롱번호로 정확 매칭해 동음이의어(예: בָּרָא 창조 H1254A vs 살찌다 H1254B) 오류를 막는다. **사전 정의도 기억으로 쓰지 말고 이 옵션으로 추출**하고, 없거나 모호하면 자동 표시되는 `[확인 필요]`를 존중한다. 최초 1회 `python src/build_lexicon.py`로 사전 데이터 설치.
   - 3단계: 신학·역사·상호본문 (Hays 기준·LXX·정경 궤적)
   - 4단계: 설교 프레임워크 (담화 강조점·장르 문법→대지·상황화·구체 행동)
   - 각 단계 끝에 "품질 점검"(확신도·자기수정).
   - **장르 계약**: 1단계에 `장르 판정 (Genre Verdict)`을 독립적으로 쓴다. 유효 값은 `내러티브 | 시가·지혜 | 서신 | 예언·율법 | 묵시 | 혼합(주+부)`이며, 문학 표지 한 가지 이상을 근거로 든다. 판단이 어려워도 값은 비워 두지 말고 `혼합(주+부)`로 쓰며, 근거 문장에 `[확인 필요]`를 붙인다. `[확인 필요]` 자체는 장르 판정값이 아니다. 4단계의 개요는 이 판정을 따른다. 내러티브는 사건의 긴장·전환·해결을, 시가·지혜는 이미지·병행·화자의 호소를, 서신은 주장·근거·함의를, 예언·율법은 언약 상황·고발/약속/명령을, 묵시는 환상·상징·목회적 소망을 중심으로 전개한다. 모든 본문을 같은 3대지 틀에 억지로 넣지 않는다.

3. **완료 전 감사**
   - 4단계 전체 주해는 Markdown **초안**으로 저장할 수 있지만, 완성본 선언·공유·워드 내보내기 전에는 `harness/exegesis_audit.md`를 따른다. 감사 뒤 초안이 바뀌면 다시 감사한다.
   - 구조 결함, `FAIL`, `보류(HOLD)`이면 완성본으로 선언·공유하거나 내보내지 않는다. `WARN`은 모든 사실 주장이 `PASS`인 상태에서 남은 비사실적 고지·확인 과제만 있을 때에만 사용자 동의로 완성본 공유·내보내기를 진행할 수 있다. 오류 사실, 무근거 직접 인용, 정확한 단락 경계 주장은 `FAIL`이며 동의로 넘기지 않는다.
   - **설교(4단계) 예화 감사 강도** — 설교는 강단 선포라 예화·인문 인용을 본문·원어처럼 빡세게 잡으면 강단 언어가 위축된다. 감사 전 **강도를 한 번 묻는다**(미지정 시 `표준`): `강단용`(예화·인용은 감사 제외, "출처는 설교자가 직접 확인" 고지) / `표준`(실존 인물·작품·통계는 `[확인 필요]`, 명백한 오류만 `FAIL`, 익명 예화는 통과) / `출판용`(설교집·기고면 예화 인용도 출처 요구). **본문·원어·사전·관주는 강도와 무관하게 항상 엄격**하다. 신학·적용의 옳고 그름은 어느 강도에서도 판정하지 않는다(율법주의·번영복음 경향은 `WARN` 고지만). 상세는 `harness/exegesis_audit.md`.

4. **분량 모드**
   - 기본: 4단계 전체 주해.
   - 사용자가 "간략히/묵상"이라 하면 1·4단계 중심으로 축약.

5. **산출물 저장**
   - `output/<구절>_주해.md`로 저장한다. 감사 전 파일은 초안이며, 감사 통과 뒤 내용이 바뀌면 재감사한다.
   - 말미에 고지: "본 분석은 연구·묵상 보조이며 목사·신학자의 권위를 대체하지 않습니다. 원어·신학 쟁점은 권위 자료로 교차 확인하십시오."
   - **워드(.docx) 내보내기** — 사용자가 "워드로/한글로/문서로 뽑아줘"라고 하면:
     ```bash
      python src/exegesis_state.py register "output/<구절>_주해.md" --ref "<구절>"
      python src/export_exegesis_docx.py "output/<구절>_주해.md"      # 단일
      python src/export_exegesis_docx.py --all --overwrite                    # output/ 전체
      ```
      상태 등록 뒤 구조·문헌학·신학·설교 네 단계를 모두 `complete`로 기록하고, 같은 해시의 `PASS` 또는 모든 사실이 `PASS`이며 정확한 동의 파일이 해시로 기록된 `WARN`만 래퍼가 내보낸다. `--all`은 전부 사전검사해 하나라도 불가하면 하나도 변환하지 않는다. 기존 `.docx`는 `--overwrite` 없이는 거부한다. 일반 `src/export_docx.py`는 호환용 변환기이며 이 게이트를 우회하는 완료 선언에 쓰지 않는다.

## 모드: 교회력(절기) 본문

사용자가 "부활절 / 성탄절 / 고난주간 / 사순절" 등 **절기**를 말하면:
```bash
python src/liturgical.py             # 전체 절기 목록
python src/liturgical.py "부활절"      # 해당 절기 핵심 본문 + 본문 텍스트
python src/liturgical.py "고난주간" --refs-only   # 구절만 → 4단계 주해로 연결
```
- 절기 핵심 본문을 제시하고, 사용자가 "이 중 ○○ 주해해줘" 하면 그 구절로 4단계 분석.
- "이번 부활절 설교 본문 잡아줘" 류 → 절기 본문 제시 + 추천 + 선택 구절 주해.
- ⚠️ 절기-본문 매칭은 전통(RCL·한국 개신교)이라 교파별로 다를 수 있음을 한 줄 고지.

## 모드: 단어·주제 연구

"사랑/믿음/은혜 같은 주제" 또는 "○○라는 단어가 성경에 몇 번/어디" 요청 시:
```bash
python src/word_search.py G26          # 스트롱번호(헬라어 사랑 ἀγάπη)
python src/word_search.py H2617        # 히브리어(헤세드)
python src/word_search.py G26 --refs   # 구절만 → 대표 구절 주해로 연결
python src/search.py "은혜"             # 번역 본문(개역한글 등)에서 한국어 단어 검색
python src/search.py "믿음" --book 롬    # 특정 책만 (원어 스트롱은 word_search, 번역 단어는 search)
```
- 출현 분포(책별 빈도)를 보여주고, 신학적 의미를 종합. 대표 구절은 4단계 주해로 연결.
- 한국어 주제어 → 스트롱번호는 분석 AI가 매핑(예: 사랑=ἀγάπη G26 / 헤세드 H2617).

## 모드: 강해 설교 시리즈 기획

"○○서 강해 시리즈 / 연속 설교 / 책별 성경공부 계획" 요청 시:
```bash
python src/series.py "빌립보서"     # 단락(회차)별 골격
```
- 책을 페리코페(소제목) 단위로 나눠 N회 시리즈 골격을 제시한다. 소제목 포함 본문이 없으면 책 전체를 임의의 한 회차로 만들지 않고, 합법적으로 준비한 소제목 포함 본문을 요청한다. 각 회차를 4단계 주해로 전개.

## 모드: 병행 본문 비교

공관복음 등 병행 본문(예: 같은 사건이 마·막·눅에) 요청 시:
- `lookup.py`의 소제목에 병행 표시가 있음(예: "<살아나시다(막 16:1-8; 눅 24:1-12)>").
- 각 병행 본문을 추출해 **나란히 비교**(공통점·차이점·각 복음서 강조점)하고, 필요 시 원어로 차이 확인.

## 모드: 큐티/묵상 (가벼운 모드)

"○○ 묵상 / QT / 오늘 말씀" 요청 시 — 4단계 풀 주해 대신 가볍게:
1. **본문** (lookup 인용) 2. **관찰**(무엇을 말하나) 3. **해석**(무슨 뜻인가, 필요 시 원어 한 단어) 4. **적용**(오늘 내 삶에) 5. **기도 한 문장**.

## 모드: 성경공부 교재

"○○ 성경공부 교재/워크북/인도안" 요청 시:
- 본문(lookup) + **도입 질문** + **관찰 질문**(본문에 무엇이) + **해석 질문**(왜·무슨 뜻) + **적용 질문**(내 삶) + 인도자 노트(3단계 신학 배경 요약).

## 모드: 통독 가이드

"○○장 통독 / 통독 가이드" 요청 시:
- 해당 장(lookup)의 **핵심 흐름·등장인물·주제**를 요약하고, 통독 시 놓치기 쉬운 포인트와 묵상 질문 1~2개.

## 모드: 성경 배경 연구 (인물·사건·여정·제도)

"출애굽 배경 / 바울 선교여행 / 다윗 생애 / 성막 제도" 등 역사·지리 배경 요청 시:
```bash
python src/background.py                 # 주제 목록(논쟁도 표시)
python src/background.py "출애굽"         # 핵심 구절 + 논쟁도
```
**반드시 3층으로 분리**한다:
- ① **성경이 말하는 것** — 본문(lookup 인용, 확실)
- ② **역사·지리** — 출처 있는 것만. 연대·지명 비정은 `[견해차]` 표시
- ③ **신학적 의미** — 설정 전통(개혁주의 등) 기준

**논쟁도(controversy) 처리** — 이게 핵심 안전장치:
- `high`(출애굽 역사성·다윗 실존·가나안 정복 등) → ②역사층에서 **보수신학 vs 비평학 양쪽을 반드시 병기**. 한쪽을 사실로 단정 금지. "성경은 ~라 말하고(①), 역사학은 ~논쟁이 있다(②)"로 명확히 구분.
- `low`(바울 여행 등) → 비교적 안전하나 역시 출처 없는 단정 금지.
- 고고학·연대 세부는 권위 자료(주석·성서지리서) 교차 확인 권장 안내.
- 예시 산출물: output/바울_선교여행_루트.md

## 모드: 주제·자료 기반 설교 (본문 선정 → 설교문)

"○○ 주제로 설교", "이 기사/논문/칼럼 바탕으로 설교", 자료를 붙여넣고 "설교문 만들어줘" 요청 시. 구절이 아니라 **주제나 자료에서 출발**해 본문을 정하고 설교문까지 가는 모드. 목사가 전도사에게 시키듯 주제·자료를 주면 본문 선정부터 설교 원고까지 잇는다.

### 절차
1. **입력 유형 판별**: (a) 주제 한 줄("용서", "번아웃") (b) 자료 전문(기사·칼럼·논문 붙여넣기) (c) 파일 경로. 자료면 핵심 논점·메시지·키워드를 먼저 한국어로 요약한다. **자료 원문은 저장·재출력하지 않는다**(저작권·개인정보).
2. **본문 후보 검색 (환각 방지, 필수)**: 기억으로 구절을 대지 말고 도구로 실제 검색한다. **한 표현에 그치지 말고 유의어·반대어·상위개념으로 넓혀** 검색한다(예: "쉼"이면 안식·쉬게·평안·수고·멍에 — 주제어 하나로는 핵심 본문을 놓친다).
   - `python src/search.py "<주제어>"` — 번역 본문에서 주제어가 실제 나오는 구절 + 책별 분포
   - `python src/word_search.py <스트롱>` — 원어 개념어(예: 사랑 G26, 헤세드 H2617)로 추적
   - `python src/xref.py "<후보구절>"` — 관련 구절로 확장
   - 주제어→스트롱 매핑은 분석 AI가 하되, **구절 존재·본문은 반드시 도구 출력으로 확인**한다.
3. **후보 제시 + 목사 선택 (독단 금지)**: 검색으로 나온 본문 3~5개를 제시한다. 각 후보에 ① 실제 본문(`lookup` 인용) ② 주제와의 연결점 ③ 설교 방향의 결(장르·톤)을 한 줄씩. **최종 본문 선택은 목사가 한다** — 설교 본문 선정은 목회·신학 판단이므로 AI는 확정하지 않고 추천·근거만 댄다. 모호하면 한 번 되묻는다.
4. **선택 본문 4단계 주해**: 확정 본문으로 `METHODOLOGY.md` 1~4단계. 자료에서 뽑은 논점은 4단계 상황화(Bridge)·적용에 반영하되, **본문에서 도출되지 않은 주장을 본문에 억지로 싣지 않는다**(본문이 자료를 예증하는 도구로 전락하는 eisegesis 금지 — 본문 자체의 메시지가 우선).
5. **설교 원고 완성**: 4단계 뼈대(중심메시지·대지·상황화·행동·Q&A)를 실제 강단 원고로 전개한다. 서론(도입 예화·질문) → 본문 강해(대지별) → 적용 → 결단·기도 흐름. 예화·인문 인용의 실존 사실은 **설교 감사 강도(강단용/표준/출판용)** 를 따른다. 분량은 설교 시간(예: 20분 ≈ 3000자)을 물어 맞춘다.
6. **완료 전 감사**: 분석 절차 3항과 동일. 본문·원어·관주는 강도 무관 엄격, 예화는 강도 조절, 신학·적용 옳고 그름은 판정하지 않는다.

### 이 모드 철칙
- 본문은 도구 검색으로 실제 확인(기억 인용 금지). 주제에 맞다고 없는 구절·엉뚱한 장절을 지어내지 않는다.
- 본문 최종 선택은 목사. AI는 후보·근거·결만 제시한다.
- **본문이 자료를 위한 증빙으로 전락하지 않게** — 자료의 주장이 성경 본문 위에 서지 않도록, 본문 자체의 메시지를 우선한다.
- 자료 원문·개인정보는 산출물·감사 증거·파일명에 남기지 않는다.

## 안전 철칙 (절대)

- 본문 = `lookup.py` 인용만. 기억 인용 금지.
- 원어·스트롱넘버 = 불확실하면 `[확인 필요]`. 지어내지 않는다. **사전 정의는 `--lex`로 데이터에서 추출**(기억 금지); 사전에 없거나 동음이의어로 모호하면 `[확인 필요]`.
- 신학 = 설정 전통 중심, 쟁점은 병기. 이단·극단 해석 차단.
- 저작권 본문(개역개정·NA28 등)을 산출물에 통째로 대량 전재하지 않는다(인용 범위 준수).
- **원어 표기 위치**: 히브리어·아람어처럼 **오른쪽→왼쪽으로 쓰는 문자**의 *원문*은 표·본문 줄·문장 속 인용에만 넣고, **고정폭 구조 도표(```)에는 원문 대신 음역·번역**을 쓴다(RTL+고정폭은 줄맞춤이 깨진다). 원어의 정밀 정보(파싱·스트롱번호)는 표에서 제시한다. 헬라어는 왼쪽→오른쪽이라 도표에 넣어도 무방.
